@model IEnumerable<HelenaGerber_Promo.Models.HGStore.ImageStore>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FileName1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FileName2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FileName3)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FileName1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FileName2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FileName3)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>

<div>
    @Html.ActionLink("Назад в панель управления", "Index", "CP")
</div>